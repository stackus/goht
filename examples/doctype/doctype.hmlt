// Adds a doctype to the top of the page
// HTML5 doctype is the default
@hmlt Doctype1() {
!!!
}

// Indenting the Hamlet code is optional; however, if you do or
// don't keep it consistent throughout the project
@hmlt DoctypeIndented() {
	!!!
}
